{
  "name": "decentrathon",
  "version": "1.0.0",
  "description": "A CLI tool for Greenfield",
  "main": "dist/cli.js",
  "bin": {
    "gnfd-cmd": "dist/cli.js"
  },
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^10.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "typescript": "^5.0.4"
  }
}
