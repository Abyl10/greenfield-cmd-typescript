{
  "name": "decentrathon",
  "version": "1.0.0",
  "description": "A CLI tool for Greenfield",
  "main": "dist/cli.js",
  "bin": {
    "gnfd-cmd": "dist/cli.js"
  },
  "scripts": {
    "build": "npx tsc src/cli.ts --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bnb-chain/greenfield-chain-sdk": "^0.2.0",
    "@bnb-chain/greenfield-cosmos-types": "^0.4.0",
    "commander": "^10.0.1",
    "dotenv": "^16.0.3",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "typescript": "^5.0.4"
  }
}
